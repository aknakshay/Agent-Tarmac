name = "agent-tarmac-telemetry"
main = "worker.js"
compatibility_date = "2024-11-01"

# Created by: wrangler kv namespace create TARMAC_KV
# then paste the printed id below.
[[kv_namespaces]]
binding = "TARMAC_KV"
id = "REPLACE_WITH_KV_NAMESPACE_ID"

# Secrets (set with `wrangler secret put <NAME>`, do NOT commit them):
#   STATS_TOKEN    required to read GET /stats
#   GITHUB_TOKEN   optional; raises the GitHub API rate limit to 5000/hr
